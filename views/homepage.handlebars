<ol class="post-list">
    {{#each posts as |post|}}
    <li>
        <article class="post">
            <div>
                <h2 class="post-title">{{post.title}}</h2>
                <h3 class="date-created">{{post.date_created}}</h3>
                <p class="post-content">{{post.content}}</p>                
            </div>
        </article>
    </li>
    {{/each}}
</ol>